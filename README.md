This project is a demo of build a simple bigram language model from scratch to understand Decoder Only Arrchitect.
In Production we didn't use it, we finetune GPT pretrained model for higher accuracy.